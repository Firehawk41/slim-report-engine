# Copy to .env and fill in values. .env is gitignored — never commit real values here.

# SQLite (dev)
DB_URL=sqlite:///report_engine.db

# PostgreSQL (prod)
# DB_URL=postgresql+psycopg2://user:pass@host/dbname
